#/bin/sh

PROJECT_ROOT=$( cd "$( dirname "${BASH_SOURCE[0]}" )/.." && pwd )

PYTHON=python
echo Script run-tests is running in `pwd`
echo PROJECT_ROOT = $PROJECT_ROOT
# ${PYTHON} -m unittest discover tests
${PYTHON} app-test.py
